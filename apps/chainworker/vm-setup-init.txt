sudo apt update
sudo apt full-upgrade -y
sudo apt install ca-certificates curl gnupg -y

sudo apt install git -y

git config --global user.name "David Ferris"
git config --global user.email "david.j.ferris.21@gmail.com"
git config --global init.defaultBranch main

git --version

curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash
source ~/.bashrc
command -v nvm
nvm install --lts
nvm alias default lts/*
nvm install 22
nvm use 22


ssh-keygen -t ed25519 -C "chainworker-gcp"
add __.pub to https://github.com/settings/keys

corepack enable