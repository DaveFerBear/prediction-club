// Shared treasury tap policy for all agents.
// Keep these global so every agent follows the same funding behavior.
export const AGENT_TREASURY_MIN_USDC = '2.00';
export const AGENT_TREASURY_TARGET_USDC = '5.00';
